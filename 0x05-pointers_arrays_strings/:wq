#include "main.h"
/**
 * print_rev - reverse string
 * @s: string
*/
void print_rev(char *s)
{
	int c = 0;
	int i;

	while (*s != '\0')
	{
		c++;
		s++;
	}

	for (i = c; i >= 1; i--)
	{
		_putchar(*s);
		s--;
	}
	_putchar('\n');
}
