#include <stdio.h>
/**
 * main - starting point
 *
 * Return: 0 if successful
 */
int main(void)
{
	int number = 48;

	while (number <= 57)
	{
	putchar(number);
	number++;
	}
	putchar('\n');
	return (0);
}
