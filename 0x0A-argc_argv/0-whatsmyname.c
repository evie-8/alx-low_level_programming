#include <stdio.h>
/**
 * main - entry point
 * @argc: counts number of arguments from command line
 * @argv: a string that stores the arguments passed from command line
 * Return: 0 if success
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
